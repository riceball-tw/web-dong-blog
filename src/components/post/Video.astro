---
export interface Props {
  width: number;
  height: number;
  data: Array<{
    src: string;
    type: string;
  }>;
}

const { width, height, data } = Astro.props;
---

<video width={width} height={height} autoplay muted playsinline loop>
  {
    data.map((source: { src: string; type: string }) => (
      <source src={import.meta.env.BASE_URL + source.src} type={`video/${source.type}`} />
    ))
  }
  <p>你的瀏覽器不支援影片，請更新瀏覽器。</p>
</video>
