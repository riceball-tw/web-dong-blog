---
import BlogBase from '@/layouts/BlogBase.astro';
import Hero from '@/components/app/Hero.astro';
import ShortpostList from '@/components/atoms/shortpost-list/ShortpostList.astro'
import Container from '@/components/atoms/container/Container.astro';
import { dateSortedPublishedShortposts } from '@/utils/getShortposts.ts';
import * as m from "@/paraglide/messages";
---

<BlogBase title={m.sea_same_gopher_burn()} description={m.round_even_leopard_drip({number: dateSortedPublishedShortposts.length})}>
  <Hero>
    <Fragment slot="headline">
      {m.round_even_leopard_drip({number: dateSortedPublishedShortposts.length})}
    </Fragment>
  </Hero>
  
  <Container>
    <section>
      <h2 class="sr-only">{m.spicy_quiet_ibex_clasp()}</h2>
      <ShortpostList shortposts={dateSortedPublishedShortposts}>
      </ShortpostList>
    </section>
  </Container>
</BlogBase>